import {
  cilApplicationsSettings, cilGroup, cilHome, cilLayers,
  cilMenu, cilMoney, cilNotes, cilPen,
  cilSpeedometer, cilUser, cilUserPlus, cilWallpaper, cilXCircle,
} from '@coreui/icons';

export const iconSubset = {
  cilApplicationsSettings,cilHome,
  cilMenu,cilGroup,cilNotes,cilUserPlus,
  cilSpeedometer,cilMoney,cilLayers,
  cilPen,cilXCircle,cilWallpaper,cilUser
};

export enum IconSubset {
  cilApplicationsSettings = 'cilApplicationsSettings',
  cilMenu = 'cilMenu',
  cilSpeedometer = 'cilSpeedometer',
}
